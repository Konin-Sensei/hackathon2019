#pragma once
#
#include <string>

bool Issue[100];

std::string categorgy[5] = ["Engine", "Steering", "Transmission", "Brakes", "Misc"];

bool isIssue()
{
	for (int i = 0; i < issue.size() - 1; i++)
	{
		if (issueList[i] = true;
		{
			return true;
		}
		else
		{
		}
	}
}

